# Vela Engine — Python dependencies
# Install inside a venv:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# WebSocket server
websockets>=12.0

# HTTP client (used by inference.py to call the llama-server OpenAI API)
aiohttp>=3.9

# VAD (Silero)
torch>=2.2          # CPU build is fine; the GPU work is done by llama-server
torchaudio>=2.2
numpy>=1.26
silero-vad>=5.0     # pip install silero-vad  (installs the model automatically)

# TTS (Piper)
# On Arch you can also install piper-tts from the AUR: yay -S piper-tts
# The pip package works too:
piper-tts>=1.2.0

# NOTE: after installing piper-tts, download your voice model, e.g.:
#   mkdir -p ~/piper-models
#   cd ~/piper-models
#   wget https://huggingface.co/rhasspy/piper-voices/resolve/main/it/it_IT/paola/medium/it_IT-paola-medium.onnx
#   wget https://huggingface.co/rhasspy/piper-voices/resolve/main/it/it_IT/paola/medium/it_IT-paola-medium.onnx.json
# Then set PIPER_MODEL in text-to-speech.py (or via the env var VELA_PIPER_MODEL).
