# Vela Orchestrator — Python dependencies
# Install with:  pip install -r requirements.txt  (inside venv)

# ── Web & WebSocket ────────────────────────────────────────────────────────────
flask>=3.0,<4
websockets>=12.0,<13

# ── Auth ──────────────────────────────────────────────────────────────────────
bcrypt>=4.1
PyJWT>=2.8

# ── Wake word detection ───────────────────────────────────────────────────────
openwakeword>=0.6
# openwakeword brings in onnxruntime, numpy and tflite-runtime automatically.
# If you prefer the tflite backend, set VELA_INFERENCE=tflite

# ── Async HTTP (router → detector) ───────────────────────────────────────────
aiohttp>=3.9
